enable-pre-post-scripts=true
# Allow lifecycle scripts for specific packages that need native compilation
# Required for sharp (image processing) and other build-dependent packages
ignore-scripts=false