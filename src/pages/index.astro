---
import { type CollectionEntry, getCollection } from "astro:content";
import SocialList from "@/components/SocialList.astro";
import PostPreview from "@/components/blog/PostPreview.astro";
import Note from "@/components/note/Note.astro";
import { getAllPosts } from "@/data/post";
import PageLayout from "@/layouts/Base.astro";
import { collectionDateSort } from "@/utils/date";

// Posts
const MAX_POSTS = 10;
const allPosts = await getAllPosts();
const allPostsByDate = allPosts
	.sort(collectionDateSort)
	.slice(0, MAX_POSTS) as CollectionEntry<"post">[];

// Notes
const MAX_NOTES = 5;
const allNotes = await getCollection("note");
const latestNotes = allNotes.sort(collectionDateSort).slice(0, MAX_NOTES);

const elingProject: { desc: string; href: string; title: string }[] = [
	{
		desc: "AI app to recognize several categories of foods and their calories.",
		href: "https://github.com/husain502/Careproject",
		title: "CARE (Camera Calories)",
	},
	{
		desc: "Simple ML algorithms from scratch using Python.",
		href: "https://github.com/elingp/ml-from-scratch",
		title: "ML from Scratch",
	},
	{
		desc: "SPA boilerplate using vanilla JavaScript and Webpack 5.",
		href: "https://github.com/elingp/vanilla-spa-webpack-boilerplate",
		title: "SPA Webpack Boilerplate",
	},
	{
		desc: "Forum-style programming Q&A website.",
		href: "https://github.com/elingp/tanja-pemrograman",
		title: "Tanja Pemrograman",
	},
	{
		desc: "Internal study resources aggregator website.",
		href: "https://github.com/elingp/seenau",
		title: "Seenau",
	},
	{
		desc: "Health Center patient information management website.",
		href: "https://github.com/elingp/sistem-informasi-puskesmas",
		title: "Sistem Informasi Puskesmas",
	},
];
---

<PageLayout meta={{ title: "Home" }}>
	<section>
		<p class="mb-4">
			Hi! I’m <b>Eling</b> (he/him). I write about my hobbies in manga/anime and TTRPG, my fascination
			with tech, and random stuff. While I’m still discovering my niche, you’ll find a bit of everything
			that captures my interest. Enjoy!
		</p>
		<SocialList />
	</section>
	<section class="mt-16">
		<h2
			class="title text-accent active:bg-link/10 mb-6 text-xl underline-offset-2 hover:underline hover:decoration-2 active:decoration-dotted md:text-2xl"
		>
			<a href="/posts/">Most recent 10 posts</a>
		</h2>
		<ul class="space-y-4" role="list">
			{
				allPostsByDate.map((p) => (
					<li class="grid gap-2 sm:grid-cols-[auto_1fr]">
						<PostPreview post={p} />
					</li>
				))
			}
		</ul>
	</section>
	{
		latestNotes.length > 0 && (
			<section class="mt-16">
				<h2 class="title text-accent active:bg-link/10 mb-6 text-xl underline-offset-2 hover:underline hover:decoration-2 active:decoration-dotted md:text-2xl">
					<a href="/notes/">Most recent 5 notes</a>
				</h2>
				<ul class="space-y-4" role="list">
					{latestNotes.map((note) => (
						<li>
							<Note note={note} as="h3" isPreview />
						</li>
					))}
				</ul>
			</section>
		)
	}
	<section class="mt-16">
		<h2 class="title text-accent mb-6 text-xl md:text-2xl">Public projects</h2>
		<ul class="space-y-4" role="list">
			{
				elingProject.map(({ desc, href, title }) => (
					<li class="grid gap-1 sm:grid-cols-[auto_1fr]">
						<a class="cactus-link min-w-66" href={href} rel="noreferrer" target="_blank">
							{title}
						</a>
						{desc}
					</li>
				))
			}
		</ul>
	</section>
</PageLayout>
