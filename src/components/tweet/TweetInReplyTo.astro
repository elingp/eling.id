---
import type { EnrichedTweet } from "./api.ts";

interface Props {
	tweet: EnrichedTweet;
}

const { tweet } = Astro.props;
---

<a href={tweet.in_reply_to_url} class="tweet-in-reply-to" target="_blank" rel="noopener noreferrer">
	Replying to @{tweet.in_reply_to_screen_name}
</a>
